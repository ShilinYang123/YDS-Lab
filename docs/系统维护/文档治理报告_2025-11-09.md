# 文档治理报告（2025-11-09）

## 概述
本次治理围绕《规范与流程.md》修订展开，目标是统一公司、部门、项目与系统文档的路径规范，完成旧路径引用清理与计划任务落地，并建立后续自动化治理基础。

## 已完成事项
- 统一路径与索引：
  - 公司级索引已更新：`S:\YDS-Lab\01-struc\docs\README.md`，覆盖 01-战略规划/02-组织与流程/03-技术文档/04-对外文档/05-模板库/06-流程与交付/07-资料库。
  - 顶层大写 `Docs/` 已合并到小写 `docs/`，源目录已清理。
- 引用修复：
  - 旧路径 `docs/公司/*` → 统一迁移为 `S:\YDS-Lab\01-struc\docs\*`（编号中文目录分类）。
  - 旧路径 `docs/规范与流程.md` → 统一为 `S:\YDS-Lab\01-struc\docs\02-组织与流程\规范与流程.md`。
  - 旧备份命名模式 `backups/daily/d_YYYYMMDD_HHMMSS` → 统一为 `backups/daily/YYYYMMDD-HHmmss.zip`。
- 备份配置增强：
  - `config/backup_config.json` 已支持全局与逐源排除列表（excludeDirs/excludeFiles），`tools/archive/daily_backup.ps1` 按配置动态构建 `/XD`、`/XF`。
- 计划任务创建：
  - 每日备份：`YDS-DailyBackup`，每日 02:00，保留 14 天。
  - 部门索引：`YDS-DeptDocsIndexDaily`，每日 00:10，自动生成 INDEX.md（五类标准：规章制度/流程与SOP/模板/会议纪要/项目资料）。
  - LongMemory 巡检：`YDS-LongMemory-HealthCheck`，每 30 分钟，调用稳定脚本 `tools/LongMemory/health_check.ps1`。

## 验证结果
- 旧路径残留扫描：
  - `docs/公司`：代码与文档层已清理；仅 tmp 恢复日志存在历史引用（非现行路径）。
  - `docs/规范与流程.md`：未发现残留。
  - 旧备份命名模式：未发现残留。
- LongMemory 健康检查：
  - 手动执行成功：`tools/LongMemory/health_check.ps1` 写入快照 `S:\YDS-Lab\01-struc\0B-general-manager\logs\longmemory\health\health_20251109.jsonl`，并回写至 TraeLM（返回 id）。
- 任务注册日志：见 `S:\YDS-Lab\docs\系统维护\计划任务创建日志_20251109-223051.txt`。

## 后续工作与建议
1. 结构一致性检查：运行 `tools/check_structure.py`，输出报告到 `docs/系统维护`，持续监控路径与命名规范。
2. 备份试运行：触发一次小范围试运行，检查排除规则与 ZIP 命名模式；将执行结果与清单文件写入 `docs/系统维护`。
3. 顶层新人导航：在 `docs/` 目录新增《新人引导.md》，提供统一入口与常用链接。
4. 历史日志清理：逐步清理 tmp 下历史报告；保留关键迁移日志作为治理佐证。

## 变更留痕与附录
- 计划任务创建操作已在 `计划任务创建日志_20251109-223051.txt` 记录。
- LongMemory 快照日志位于：`01-struc\0B-general-manager\logs\longmemory\health\health_20251109.jsonl`。
- 本报告作为《规范与流程》修订后的执行记录，后续更新将按日期追加。